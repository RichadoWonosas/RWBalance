# RW Balance 实现状态清单

> 对照文档：需求确认稿 v1.2
> 检查日期：2026-09-13
> 标记规则：`[x]` 表示当前代码已形成可运行闭环；`[ ]` 表示未实现或仅部分实现，括号内说明现状。
> 对齐规则：以需求确认稿第 13 节“已确认的需求决策”为冲突裁决依据，并以用户补充的原始需求校验页面职责与技术选型。

## 0. 原始需求对齐修正

- [x] 登录页账本条目仅显示名称、“修改名称”和“删除”，不再泄露更新时间或其他账本信息
- [x] Dashboard、桌面全局框架和账目页共用同一个批量添加账目弹窗
- [x] 批量添加界面不再作为账目查看页中的常驻编辑区
- [x] 账户恢复使用同一更正组追加“反向恢复记录 + 0 金额替代记录”
- [x] 领域层独立拒绝负数、非安全整数及非法币种金额，不依赖 HTML 输入约束
- [x] Tailwind CSS 实际承担页面和组件常规样式（应用外壳、全部页面、主要大型弹窗、字体、语义色和移动优先断点均已迁移）

## 1. 工程与基础架构

- [x] Vue 3、TypeScript、Vite 前端工程
- [x] Tailwind CSS 4 构建管线
- [x] Tailwind 工具类及 Tailwind 基础组件的页面级迁移（全部页面、账目录入、账户/标签/主题/迁移向导及通用 Modal 外壳已完成）
- [x] 响应式桌面端与移动端页面框架
- [x] 领域规则与 Vue 界面分离
- [x] IndexedDB 本地持久化层
- [x] 版本化加密账本容器
- [x] Web Worker 数据层（解密、完整账本状态、领域命令、余额投影、查询聚合、加密校验与持久化均在专用 Worker 中执行）
- [x] Vue Router 路由层（Hash 路由、业务页 URL 与未解锁访问守卫）
- [x] Pinia 独立状态管理层（仅保存账本索引、页面投影视图和会话状态）

## 2. 多账本与生命周期

- [x] 本机多账本列表
- [x] 未解锁账本条目只显示账本名称、修改名称和删除操作
- [x] 按账本名称即时搜索（搜索图标使用 CSS 矢量绘制并与输入框几何居中）
- [x] 创建账本并设置访问口令
- [x] 口令确认不一致时阻止创建
- [x] 解锁账本
- [x] 错误口令和损坏容器的统一安全错误提示
- [x] 同一标签页仅保持一个已解锁账本
- [x] 锁定后清除当前账本和口令引用
- [x] 删除账本前重新验证口令并确认
- [x] 设置页两阶段修改账本名称并重新验证口令
- [x] 登录页直接修改账本名称
- [x] 账本切换专用流程（终止当前 Worker 会话后返回账本选择页）
- [x] 默认 300 秒、可按账本设置的无操作自动暂离；`pagehide` / `beforeunload` 主动终止 Worker
- [x] “暂离”清除 Worker 中的解密数据并保留当前页面与未提交草稿，同账本重新解锁后恢复
- [x] “退出”清除解密会话、弹窗状态及所有未提交临时内容
- [x] 删除前一键导出密文备份

## 3. 文件导入、导出与外观元数据

- [x] 导出完整 `.rwbl` 加密账本文件
- [x] 导入 `.rwbl` 并在写入前验证访问口令和认证标签
- [x] 文件选择区支持点击选择、文件名反馈和 `.rwbl` 拖拽上传
- [x] 导入时不生成明文中间文件
- [x] 主色、副色和明暗色调存放在未加密容器头
- [x] 外观设置使用不超过 4 个 base36 字符的紧凑编码
- [x] 外观元数据加入 AES-GCM 附加认证数据，篡改后无法解锁
- [x] 旧版无公开外观字段的容器保持兼容
- [x] 同一账本 ID 导入时选择“副本”或“替换”（同名不同 ID 仅允许生成自动改名的副本）
- [x] 替换账本前自动下载现有密文备份，并写入 IndexedDB 上一版本恢复区
- [x] 外部修改文件的完整业务约束校验（ID、名称、金额、币种、账户/标签/审计引用、余额和设置）

## 4. 主题与界面体验

- [x] 默认 270° 紫色主题色和 225° 蓝色副主题色
- [x] 主色与副色均支持 0–359° 任意色相
- [x] 拖动 360° 色环选择颜色
- [x] 色相数字输入
- [x] `±1`、`±15`、`±60` 色相微调
- [x] 每 15° 一个快捷色板
- [x] 针对不同色相插值校正饱和度和明度
- [x] 亮色调与暗色调切换
- [x] 使用独立明暗罗盘表盘素材
- [x] 主色与副色双指针显示
- [x] 指针使用参考 SVG，并通过独立 HSL 变量精确染色
- [x] 指针跨越 0° 时不使用平滑旋转
- [x] 解锁时“短促移动—停顿—加速分离—页面渐显”动画
- [x] 锁定时“页面渐隐—减速合拢—停顿—短促闭合”动画
- [x] 页签悬停、激活及页面淡入淡出动画
- [x] 对话框弹出与消失动画
- [x] 遮罩只响应从外部开始并结束的完整点击，内部拖动到外部不关闭弹窗
- [x] 暂离、退出、重新打开保留最后外观；选择账本立即读取其公开配色，新建使用默认色
- [x] 登录双区、Dashboard 渐变及桌面侧栏/移动底栏的低透明度纹理
- [x] 登录主题区使用副主题渐变与径向点纹，侧栏/移动底栏使用副主题斜向渐变叠加双向斜线纹理
- [x] 同一遮罩内的多阶段对话框切换动画（重命名、账目放弃确认、标签引用处理）
- [x] 全局按钮悬停、离开和按下状态过渡
- [x] 输入框、选择框和文件选择区的悬停、聚焦、禁用状态平滑过渡
- [x] 批量录入表单紧凑列宽及全局数字输入原生步进箭头移除
- [x] 收支账户使用可搜索的标签式折叠选择器，关闭状态不再撑高首行
- [x] 标签和账户选择器使用保留 DOM 的平滑高度折叠动画
- [x] 滚动条由主题色与副主题色共同着色
- [x] 成功、警告、错误三类提示队列（5 秒、最多 5 条、手动关闭、桌面/移动端定位）
- [x] `prefers-reduced-motion` 低动画适配
- [x] 合并低动画策略，并以浏览器测试验证键盘焦点和低动画计算样式
- [x] 样式职责拆分为主题、复杂视觉效果、动画、原生控件和页面布局入口
- [x] 远程字体移除、Tailwind 语义令牌/命名断点、移动安全区和动态视口适配
- [x] 侧栏与顶部栏组件拆分，全局 `label`/表格/导航/Tag 选择器收敛
- [x] 暗色调业务文字不使用纯白；强调文字统一为明确的 `#EEEEEE`，并有跨浏览器计算样式断言

## 5. 币种与金额规则

- [x] CNY、USD、GBP、JPY 四种币种
- [x] 金额以最小货币单位整数存储
- [x] JPY 与两位小数币种分别格式化
- [x] 金额允许为 0，拒绝负数和无效数值
- [x] 不同币种余额独立显示，不生成跨币种合计
- [x] 任一操作造成分币种负余额时拒绝提交
- [x] 批量账目整体校验、整体写入
- [x] 按币种集中配置小数位、输入上限和 half-up 舍入策略
- [x] 同币种转移两侧金额实时双向同步
- [x] 跨币种转移实时显示隐含汇率及计算方向

## 6. 账户管理

- [x] 创建多币种账户
- [x] 为每个币种生成账户初始收入记录，包括零金额记录
- [x] 标记待支出账户
- [x] 按账目投影计算账户余额
- [x] 隐藏零余额币种
- [x] 逻辑删除账户
- [x] 删除时为所有币种生成系统支出，包括零金额记录
- [x] 已删除账户不进入新账目账户选择器
- [x] 通过同一更正组追加反向记录和 0 金额替代记录，恢复账户及原余额
- [x] 编辑账户名称
- [x] 编辑账户的待支出状态且不产生资金记录
- [x] 账户名称、状态和余额分类组合筛选
- [x] 账户筛选栏为搜索、状态和余额分类提供一致的可见标题、控件底线及上下留白
- [x] 删除确认逐币种展示余额和将产生的系统支出

## 7. 标签管理

- [x] 新增标签
- [x] 标签分为“收入 / 支出”两个互斥类别；新账本预设收入类“其他收入”及支出类“出行、饮食、娱乐、生活”
- [x] 旧账本自动迁移标签类别；旧标签默认归入支出类，历史收入补入收入类兜底标签，资金与审计记录不变
- [x] 收入、支出分别使用独立手风琴分栏和筛选框，展开一栏时自动折叠另一栏
- [x] 标签名称去除首尾空白
- [x] 使用 Unicode NFKC、全半角和大小写无关规则查重
- [x] 无账目引用的标签可以删除
- [x] 有账目引用时阻止直接删除
- [x] 收入与支出账目都至少包含一个对应类别的标签和主标签，跨类别选择由 UI 与 Worker 双重拒绝
- [x] 第一个选择的标签自动成为主标签
- [x] 取消主标签后按选择顺序顺延
- [x] 显式设置其他已选标签为主标签
- [x] 标签页按收入、支出类别分别筛选名称和父级路径
- [x] 分类筛选栏的“添加”按钮保持单行；打开向导时自动带入当前类别和筛选名称
- [x] “主标签”与“任意包含标签”两种近 30 天 Top 3 已从管理页移至统计页
- [x] 批量标签添加向导：类别默认收入，可跨类别暂存多项并在最终确认时一次原子写入
- [x] 新建标签父级使用带独立搜索的单选标签选择器；向导中强制展开，账目录入中默认折叠且类别由账目类型锁定
- [x] 向导只在普通标签区显示本次暂存的新标签并前置呈现；取消向导不会写入任何标签
- [x] 输入回车选择已有标签或创建临时标签
- [x] 临时标签的新建标识、确认、取消和未选清理
- [x] 双击已选或未选标签并设为主标签，且不破坏原选择顺序
- [x] 删除有关联账目标签时逐条移除，主标签强制指定替代项
- [x] 勾选账目后批量替换为已有或临时新标签
- [x] 标签引用处理预览、去重及单次原子持久化
- [x] 单父级、多层子标签；新建和修改父级时 Worker 校验防环及跨账本引用
- [x] 标签按层级优先、同级创建时间排序；点击父标签整行展开，各级默认折叠并使用保留 DOM 的连续高度过渡
- [x] 账户选择、标签层级、高级设置、原生增强下拉框统一使用垂直居中的实心三角指示，暗色调固定为 100% 饱和度、80% 亮度；标签箭头不带按钮底板
- [x] 标签“修改”同时支持重命名和父级调整，允许保存与自身规范化结果相同的显示名称
- [x] 直接选择顺序与祖先展开集合分开存储；隐式祖先标识及取消分支确认
- [x] 批量录入、标签编辑、资金更正复用统一子标签选择器
- [x] 父级变更影响预览、用户确认、全量历史派生标签重算及独立层级审计
- [x] 删除父标签时指定子标签提升或移动，不级联删除；处理全部历史直接引用
- [x] schema v1 → v2 显式确认升级；本机解锁、导入、副本和恢复共用迁移流程
- [x] 迁移前原密文下载、独立永久升级备份、事务失败回滚与未知版本拒绝
- [x] 子标签迁移与使用说明：`docs/subtag-migration-plan.md`

## 8. 账目录入与审计

- [x] 收入、支出、转移三种账目
- [x] 一个批量添加弹窗维护多条待提交账目
- [x] 批量账目原子提交
- [x] 新账目必须记录精确到秒并包含时区偏移的发生时间，不再用提交时间代替业务时间
- [x] 同一批次使用共享提交修订号；每条账目的初始创建/修改时间取其实际暂存时间，未暂存表单取直接提交时刻
- [x] 待提交账目使用鼠标、触控或键盘拖拽握柄排序；排序时交换时间槽而非重写业务发生时间
- [x] “直接提交”一次原子写入全部暂存账目与当前未暂存表单
- [x] 收入、支出和转移的类型字段校验
- [x] 收入、支出各自对应类别的标签和主标签校验
- [x] 批量录入桌面布局为“类型 / 精确发生时间 / 对应账户 / 对应金额”同排，日期与账户不重叠
- [x] 自由文本备注和长度限制
- [x] 通过反向冲销记录删除普通账目
- [x] 账目删除使用与标签一致的文字按钮及自定义动画确认框，不调用浏览器原生确认窗口
- [x] 原账目和冲销记录均保留
- [x] 已删除账目独立视图
- [x] 通过追加恢复记录恢复已删除账目
- [x] 恢复后业务日期仍采用原记账日期
- [x] 收入账户和支出账户按名称即时搜索
- [x] 批量录入中的复制条目
- [x] 关闭未保存批量编辑时提示并允许继续编辑
- [x] 只修改标签且不生成资金流水
- [x] 使用“反向原值 + 新值记录”的资金数据更正组
- [x] 连续更正历史链详情
- [x] 对更正组按净影响而非组内顺序校验余额
- [x] 资金更正、删除冲销和恢复记录继承原始账目的发生时间，同时保留独立的创建/修改审计时间
- [x] schema v1/v2 → v3 强制发生时间迁移：按现有顺序给出建议值，用户可重排并逐条确认，未完成则保持锁定
- [x] 解锁、文件导入与上一版本恢复共用 v3 迁移门槛，升级前可导出原始密文并保留可恢复备份

## 9. 账目查询

- [x] 有效账目列表
- [x] 最近修改时间倒序展示
- [x] 修改顺序调整向导：列出修改时间精确到秒后相同的完整账目组，通过拖拽交换修改时间槽并持久化稳定次序
- [x] 已删除账目列表切换
- [x] 日期和日期范围筛选
- [x] 账目类型筛选
- [x] 账户、币种和金额范围筛选
- [x] 主标签与任意包含标签筛选
- [x] 仅直接选择 / 包含子标签 / 精确主标签 / 主标签根级汇总口径及明细继承标识
- [x] 备注关键词筛选
- [x] 创建时间和修改时间筛选
- [x] 组合筛选与清空筛选
- [x] 排序、筛选开关、顺序调整向导组成吸顶工具栏；详细筛选平滑展开，折叠时显示条件数，非默认字段独立高亮
- [x] 每页 20 条的分页列表
- [x] 完整审计记录链查看

## 10. Dashboard 与统计

- [x] Dashboard 分币种总余额
- [x] 默认近 30 天分币种收入和支出
- [x] 最近账目列表
- [x] Dashboard、桌面页面框架和账目页的全局新增账目入口
- [x] 按账户展示各币种余额概览
- [x] 储蓄与待支出余额摘要
- [x] 近 30 天、近 90 天、今年和自定义时间范围快速切换
- [x] 摘要卡片下钻到带筛选的账目页
- [x] 分账户总体余额图
- [x] 分币种收支趋势图
- [x] 跨币种换汇按来源币种计入支出、按目标币种计入收入；同币种内部转账不重复计入收支
- [x] 换汇派生不可手动选择的“换汇 / 兑换到… / 从…兑换”系统隐含标签，既有记录无需迁移即可显示
- [x] 主标签支出圆环图
- [x] 主标签与任意包含标签两种口径的支出排行
- [x] 分币种展示近 30 天“主标签支出 Top 3 / 任意包含标签 Top 3”
- [x] 标签支出区与下方分币种摘要卡片保持统一区块间距
- [x] 主标签圆环图与排行可按根级祖先互斥汇总；下钻携带口径、币种、时间范围
- [x] 跨币种转移隐含汇率折线图
- [x] 收支趋势图与换汇汇率图显示横纵坐标轴、参考网格、起止日期及纵轴范围
- [x] 折线图使用宽屏坐标系占满分栏，轴文字按正文可读尺寸显示；指针按横轴吸附最近数据点并显示悬浮信息
- [x] 主标签圆环图使用独立 SVG 扇区，悬停或键盘聚焦时放大 20% 并显示类型、金额与比例
- [x] 图表键盘摘要或等价数据表；所有等价数据表使用保留 DOM 的平滑展开/折叠
- [x] 当前统计范围的完整高度 PNG 导出
- [x] 导出预览、默认开启的重新绘制式关键信息脱敏及关闭脱敏警告

## 11. 设置与密码学

- [x] 设置页统一为“备份 / 账本安全 / 外观”三级互斥手风琴分组，使用保留 DOM 的平滑折叠动画
- [x] 设置页网格、操作栏、响应式单/双列及折叠内容间距迁移至 Tailwind；复杂主题开关视觉与动画保留原生 CSS
- [x] 导出与删除设置统一为说明栏加右侧操作按钮的设置项版式
- [x] 设置页修改账本名称
- [x] 设置页导出当前加密容器
- [x] 自动暂离时间作为账本加密设置保存（默认 300 秒）
- [x] Deflate 压缩后再加密
- [x] AES-256-GCM 认证加密
- [x] 每次保存使用随机盐和随机 IV
- [x] PBKDF2-SHA-256 参数写入容器头
- [x] 密文或外观元数据被篡改时认证失败
- [x] 修改账本访问口令（只重新封装 DEK，账本密文主体保持不变）
- [x] KDF 和加密算法选择（PBKDF2-SHA-256 / SHA-512；当前认证加密为 AES-256-GCM）
- [x] 修改 KDF 或加密算法并原子迁移容器（新容器完整验证后覆盖）
- [ ] yescrypt WebAssembly
- [ ] yescrypt 设备校准和参数固化
- [x] 独立随机 DEK 与 KEK 封装体系
- [x] 解压数据体积上限（64 MiB 流式硬限制）
- [x] KDF、加密、压缩和序列化的版本化注册表
- [x] 密文上一版本恢复机制（恢复时交换当前与上一版本，可再次回退）

## 12. 非功能与质量

- [x] TypeScript 严格类型检查
- [x] 核心余额、账户和审计规则单元测试
- [x] 主题色相规范化、校正和紧凑编码测试
- [x] 生产构建
- [x] 桌面与移动断点布局
- [x] 320/375/768/1024/1440px、六列表格横向滚动与 200% 字体布局回归
- [x] 表单字段关联标签
- [x] 主题色环的滑块语义和数值输入替代方式
- [x] 基础键盘操作和焦点样式
- [ ] 完整端到端自动化测试套件
- [x] IndexedDB 与加密容器集成测试
- [x] 子标签、标签类别、发生时间迁移、稳定批次顺序及写入失败回滚的 Worker 集成测试；当前单元/集成测试共 50 项通过
- [x] Chromium、WebKit 桌面及移动 WebKit 的 36 项端到端回归通过（2026-09-12）
- [ ] 大数据量性能与虚拟列表测试
- [x] Web Worker、Web Crypto、WebAssembly、IndexedDB 与压缩流能力检测
- [x] 弹窗完整焦点圈定、Escape 关闭及关闭后焦点恢复
- [x] WCAG 对比度自动化检查（浏览器端断言 AA 4.5:1）
- [x] 样式静态审计（未定义/无消费者变量、外部资源和 Tailwind 语义类碰撞）已接入生产构建
- [ ] Safari 和 Firefox 兼容性验证（Chromium、移动 Chromium 与 WebKit 已通过；Firefox 自动化已配置，但本机运行时因 `spawn UNKNOWN` 未能启动，将由 Linux CI 复验）

## 13. 当前建议的下一阶段

1. 接入 yescrypt WASM 与设备校准；在此之前继续明确标注 PBKDF2 安全边界。
2. 扩展端到端场景到导入冲突、账户/标签管理、批量录入、更正、恢复与统计导出。
3. 增加大数据量基准与账目虚拟列表，并为性能退化设置自动阈值。
4. 在 GitHub Actions 的 Linux Firefox 运行结果通过后，完成 Firefox 兼容性勾选。
